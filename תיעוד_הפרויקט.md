# תיעוד ועדכון — אפליקציית ניהול אישי

## 1. כללי תיעוד
- מסמך זה מתעד את הפרויקט ואת העדכונים שבוצעו בו.
- כל שינוי מתועד עם תאריך ושעה, תיאור קצר, והקשר לקוד (פונקציות/שורות).
- אסור למחוק רישומים היסטוריים — רק להוסיף חדשים.
- המסמך מתעדכן בכל סשן עבודה.

## 2. סקירת הפרויקט
אפליקציית ניהול אישי (PWA) — קובץ יחיד `index.html` (כולל CSS ו-JS מוטמעים), בעברית, RTL. כוללת:
- **טאב ניהול זמנים** — משימות/תוצאות עם תאריך, שעה, משך, חזרה (יומי/שבועי/ימי חול/חודשי/ימים ספציפיים), סטטוס השלמה, מאגר ללא תאריך, תצוגת שבוע.
- **טאב מטרות ויעדים** — תקופות, סוגי מטרות (הורה/בן), מטרות, יעדים.
- **טאב מעקב הרגלים** (חדש, 18/08/2026) — מעקב שינה וקימה עם רישום בזמן מדויק, עריכה, ממוצעים וגרף.
- חיפוש גלובלי, סנכרון דו-כיווני ל-GitHub (localStorage + GitHub API), מצב פרטיות.

## 3. ארכיטקטורת נתונים
- נתונים ב-`localStorage` מפתח `personalManagerData`.
- מבנה state: `results`, `goalPeriods`, `goalTypes`, `goals`, `objectives`, `habits`, `habitLogs`, `suggestions`, `settings`.
- סנכרון דו-כיווני ל-GitHub עם tombstones ו-LWW לפי `updatedAt||createdAt` ב-`mergeIntoLocal(cloud)`.
- מודל חזרות: משימת מקור (`sourceId: null` + recurrence) ועותקים (`recurrence: null`, `sourceId: <id מקור>`) שנוצרים ב-`autoFillRecurring(dateStr)`.

## 4. עדכונים

### 18/08/2026 — טאב "מעקב הרגלים" + הרגל מובנה "שינה וקימה"
**היקף:** קובץ `index.html` (קוד קיים + 571 שורות חדשות). Commit `c159552` (אחרי rebase), נדחף ל-GitHub.

**מה נוסף:**
1. **טאב חדש** "😴 מעקב הרגלים" (כפתור `#tabHabits` ותוכן `#tabContentHabits`) — שולב ב-`switchTab`.
2. **הרגל מובנה "שינה וקימה"**: כפתורים "אני יישן" (`logSleep()`) ו"קמתי" (`logWake()`) מתעדים את השעה המדויקת (ISO מלא, כולל תאריך).
3. **עריכת זמנים**: כפתור ✏️ בהירו של היום ובכל רשומה בהיסטוריה פותח טופס `datetime-local` לעריכת שעת השינה/קימה (`saveHabitEdit`).
4. **מחיקת רשומה** מהיסטוריה (`deleteHabitLog`).
5. **ממוצעים תקופתיים**: שבוע/חודש/שנה (`setHabitPeriod`) — ממוצע שעת שינה, שעת קימה, משך שינה, מספר לילות.
6. **גרף SVG** (`renderHabitChart`) — שני קווים (שינה סגול `#6c63ff`, קימה כתום `#f59e0b`); לשבוע/חודש נקודות יומיות, לשנה ממוצעים חודשיים; ציר Y 00:00–23:59 עם רשת כל 6 שעות.
7. **סנכרון**: `habits` ו-`habitLogs` שולבו בכל מנגנוני הסנכרון: `loadState`, `backfillTimestamps`, `buildBaseline`, `stampChanged`, `buildBackupJson`, `mergeIntoLocal`, `mergeCloudData`, `afterMergeRender`.

**מבנה נתונים של רשומת שינה (`state.habitLogs`):**
`{ id, habitId:'sleep', date: 'YYYY-MM-DD', sleepTime: ISO|null, wakeTime: ISO|null, createdAt, updatedAt }`

**הגדרת הרגל (`state.habits`):** `{ id:'sleep', name:'שינה וקימה', emoji:'😴', builtIn:true }` — נזרע אוטומטית אם חסר (ב-`loadState`, `mergeCloudData` ו-`mergeIntoLocal`).

**הערות ולוגיקות חשובות:**
- `sleepMinutesOf` ממירה שעת שינה אחרי חצות (< 12:00) ל-+1440 דקות לחישוב ממוצע/גרף תקין.
- `formatSleepDurationOf` מחשבת משך (עד 48 שעות) ומחזירה מחרוזת "X שע' ו-Y דק'".
- `renderHabitsView` מתבצעת רק כשהטאב פעיל (גארד על `active`).
- `saveHabitEdit` מוגן מפני אלמנטים חסרים (בדיקות null).

**פריסה:** נדרשת פריסה ידנית של GitHub Pages (git push ל-`master` מספיק). הסייט החי עודכן דרך ה-commit.